{
  "name": "frontier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Procedural generator for East Asian ancient-with-modern-styling buildings — phase 1: roofs",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "report": "node cli/report.mjs --obj",
    "test": "node cli/report.mjs --out out"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
